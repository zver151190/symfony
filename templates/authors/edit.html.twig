{% extends 'base.html.twig' %}

{% block body %}

<style>
    .book-items td textarea {
        width: 100% !important;
        margin: 0 !important;
    }
</style>
<div class="container">
    <div class="card" style="width: 35rem;margin: 40px auto;">
        <div class="card-header">
            Edit Author
        </div>
        <div class="card-body">
            <div id = "simpleform">
                {% form_theme form 'bootstrap_5_layout.html.twig' %}
                {{ form_start(form) }}
                {{ form_widget(form) }} 
                {{ form_end(form) }} 
            </div>
        </div>
    </div>
</div>
{% endblock %}
