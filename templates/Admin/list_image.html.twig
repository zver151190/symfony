{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    <div>
        {% if value is not null %}
            <img src="{{ value }}" width="100"/>
        {%endif%}
    </div>
{% endblock %}